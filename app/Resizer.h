#pragma once

#include <string>

class Resizer
{
public:
	Resizer(const std::string& toolPath)
	{
	}

	bool resize(const std::string& inputFilePath, int percent, const std::string& outputFilePath)
	{
		return false;
	}
};
